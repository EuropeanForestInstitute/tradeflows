---
title: "440799_discrepancies in France"
author: "Paul Rougieux"
date: "03/12/2014"
output: html_document
---

```{r packages, echo=FALSE, warning=FALSE, message=FALSE}
library(knitr)
opts_knit$set(root.dir="../../..") # file paths are relative to the root of the project directory
library(tradeflows)
library(dplyr)
``` 


```{r standalone}
load("data-raw/440799.RData")
enddata <- dtf %>%
    renamecolumns("comtrade", "efi") %>%
    addpartnerflow 
# this will have to be provided to the report generating function
```

```{r countryspecific}
reporterinreport <- 251 
enddata2 <- enddata %>%
    filter(reportercode == reporterinreport)
```

## Major trade partners in `r max(enddata)` 
```{r}

```
